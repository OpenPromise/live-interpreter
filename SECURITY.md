# 安全说明

- API Key 只存放在本地 `.env` 文件（已被 `.gitignore` 忽略），**不要提交到仓库**，也不要粘贴到公开场合。
- 如果 Key 疑似泄露，请立即到阿里云百炼控制台重置并更新 `.env`。
- 本地浮层服务默认只绑定 `127.0.0.1`，不会对局域网/公网开放。
- 直播流解析与媒体代理仅在本地使用，不对第三方提供代理服务。
- 使用声音复刻（`--voice-clone`）请确保你拥有对应声音的授权。
